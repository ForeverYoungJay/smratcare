<view class="container" wx:if="{{detail}}">
  <view class="surface card">
    <view class="top-row">
      <text class="tag {{detail.level}}">{{detail.levelText}}</text>
      <text class="time">{{detail.time}}</text>
    </view>
    <view class="title">{{detail.title}}</view>
    <view class="meta">涉及老人：{{detail.elderName}} · 类型：{{detail.type}}</view>
    <view class="content">{{detail.content}}</view>

    <view class="section">事件进展</view>
    <block wx:for="{{detail.timeline}}" wx:key="*this">
      <view class="timeline-row">{{item}}</view>
    </block>

    <view class="action-row">
      <button class="primary-btn action action-primary" bindtap="callNurse">联系责任护士</button>
      <button class="ghost-btn action action-secondary" bindtap="markRead">{{detail.type === '护理提醒' || detail.title.indexOf('护理等级调整') >= 0 ? '我已知悉本次调整' : '确认已读'}}</button>
    </view>
  </view>
</view>
