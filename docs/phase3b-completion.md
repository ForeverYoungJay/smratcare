# Phase 3B 完成说明

Phase 3B 已覆盖护理、医务、财务、后勤、行政人事、市场六个部门，以及院长总览和系统管理员入口。

## 统一规则

- 六部门工作台区分员工版与部长版。
- 所有业务数字来自现有接口；缺失个人维度时明确显示机构/团队口径及 TODO 空状态。
- 快捷操作、指标下钻、菜单、搜索和直接 URL 使用同一权限规则。
- 院长复用 `/portal` 经营风险总览；系统管理员使用 `/system` 系统治理首页。
- 不修改后端角色协议，不删除旧页面和旧地址。

## 自动验证

- 全量单测：32 个测试文件，150 项通过。
- 导航检查：634 条路由、397 个链接、0 个未解析链接。
- 生产构建：通过。
- lint：仓库当前未配置实际 lint 工具，脚本仅输出 `no lint configured`。
- typecheck：仍存在公共请求响应类型、既有查询类型等历史诊断；本阶段修改文件未新增诊断。
